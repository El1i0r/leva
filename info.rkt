#lang info

(define collection "leva")

(define deps '("base"
               "typed-racket-lib"))

(define build-deps '("racket-doc"
                     "rackunit-lib"))

(define pkg-desc "An implementation of Pie programming language from The Little Typer which expands on it's features and adds some interesting stuff.")
(define version "0.1.0")

(define pkg-authors '(
                      "Affan Ahmad <st_iaffan@outlook.com>"
                      "Eyad Amr <flamesfflowsnow@gmail.com>"
                      ))

(define license 'MIT)
